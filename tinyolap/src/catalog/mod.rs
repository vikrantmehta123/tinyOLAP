//! Catalog module — the single source of truth for table and column metadata.
//! As tinyOLAP grows to support multiple tables, schema loading, registration,
//! and lookup will live here.

pub mod schema;
